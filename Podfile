# Uncomment the next line to define a global platform for your project
platform :osx, '10.11'

target 'Daypaper' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Daypaper
  pod 'SwiftHTTP', '~> 3.0.1'
  pod 'Nuke', '~> 6.0'
  pod 'SwiftDate'
  pod 'Fuzi', '~> 2.0.0'

  pod 'Fabric'
  pod 'Crashlytics'

  target 'DaypaperTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DaypaperUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
